//Take state object from localStorage, if not, create new default
//Update interface at the beginning
const initGame = () : void =>{
    //Check if there's state saved in LocalStorage
    /*
        If true:
            -Take the location and go the the current location
            -Update action points/progressPoints/evidences
            -Update active puzzles
            -Update explored area's
            -Update story book content
        If false:
            -declare default gameState object
            -update actionPoints
    */
}