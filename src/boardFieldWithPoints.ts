import { BoardField } from './boardField';

/**
 * @Class boardFieldWithPoints extend BoardField
 *  extend normal BoardField by addPoints method, exploring this field grants user an additional action point
 */
export class BoardFieldWithPoints extends BoardField {
    addPoints(){
        // state.addpoints
    }
}
